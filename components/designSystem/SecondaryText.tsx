import { styled } from "tokens";

const SecondaryText = styled("p", {
  fontFamily: "$serif",
  fontSize: "$1",
  fontWeight: "$normal",
  color: "$secondary",
  fontStyle: "italic",
  margin: "$0",
});

export default SecondaryText;
