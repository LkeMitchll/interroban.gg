import { styled } from "../stitches";

const P = styled("p", {
  marginTop: 0,
  fontFamily: "$serif",
  marginBottom: "$1",
  maxWidth: "30rem",
  fontSize: "$1",
  lineHeight: "$default",
});

export default P;
