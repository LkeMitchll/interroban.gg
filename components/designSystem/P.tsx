import { styled } from "tokens";

const P = styled("p", {
  marginTop: "$0",
  fontFamily: "$serif",
  marginBottom: "$1",
  maxWidth: "$measure",
  fontSize: "$1",
  lineHeight: "$default",
});

export default P;
