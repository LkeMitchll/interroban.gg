<ol class="flow | gap-large">
  {% for post in posts %}
  <li class="horizontal" data-responsive>
    <a href="{{ post.url }}">
      {{ post.data.title }}
    </a>
    <hr class="grow" />
    <p class="tertiary-text">{{ post.data.date | formatDate }}</p>
  </li>
  {% endfor %}
</ol>
