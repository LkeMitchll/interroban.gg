---
layout: layouts/default
---

{% from "components/stats.njk" import stats %}

<header class="flow | gap-x-large">
  <h1 class="measure | gap-large">{{ title }}</h1>
  {{ stats(updatedDate = date, readableContent = content ) }}
  {% include "components/commits.njk" %}
</header>

<article class="markdown | gap-x-large">
  {{ content | safe }}
</article>
